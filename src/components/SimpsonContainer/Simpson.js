const Simpson = ({simpson}) => {
    const {id, name} = simpson

    return (
        <div>
            <div>id: {id} </div>
            <div>name: {name} </div>
        </div>
    );
};

export {Simpson};